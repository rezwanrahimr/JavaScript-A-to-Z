// const letter = new Set(["a", "b", "c", "d"]);
/* const a = "how";

const letter = new Set();
letter.add("a");
letter.add("b");
letter.add(a);
console.log(letter);
letter.forEach(function (value) {
  console.log("value:" + value);
});

const cal = letter.values();
for (var x of cal) {
  console.log("x" + x);
}
console.log(cal);
 */
